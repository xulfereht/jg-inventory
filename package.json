{
  "name": "jg",
  "version": "0.2.0",
  "private": true,
  "description": "한의원 한약재 재고관리 시스템",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
